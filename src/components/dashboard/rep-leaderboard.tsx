import Link from "next/link";
import { ArrowRight, Trophy } from "lucide-react";
import { formatINR, formatPct } from "@/lib/format";
import type { DashboardFilters } from "@/types/dealership";

export function RepLeaderboard({ rows, filters }: { rows: Array<{ rep: { id: string; name: string }; leads: number; delivered: number; revenue: number; conversion: number; active: number; stale: number; highIntentStale: number }>; filters: DashboardFilters }) {
  return <div className="overflow-x-auto"><table className="w-full min-w-[690px] text-sm"><thead><tr className="text-left text-[11px] uppercase tracking-[.07em] text-[#8a93a2]"><th className="border-b border-[#edf0f2] px-3 py-3 font-semibold">Rep</th><th className="border-b border-[#edf0f2] px-3 py-3 font-semibold">Leads</th><th className="border-b border-[#edf0f2] px-3 py-3 font-semibold">Delivered</th><th className="border-b border-[#edf0f2] px-3 py-3 font-semibold">Revenue</th><th className="border-b border-[#edf0f2] px-3 py-3 font-semibold">Conversion</th><th className="border-b border-[#edf0f2] px-3 py-3 font-semibold">Stale</th><th className="border-b border-[#edf0f2] px-3 py-3"></th></tr></thead><tbody>{rows.map((row, idx) => <tr key={row.rep.id} className="hover:bg-[#fafbfc]"><td className="border-b border-[#f0f1f3] px-3 py-4 font-bold"><span className="inline-flex items-center gap-2">{idx === 0 && <Trophy size={14} className="text-[#c98216]"/>}{row.rep.name}</span></td><td className="border-b border-[#f0f1f3] px-3 py-4">{row.leads}</td><td className="border-b border-[#f0f1f3] px-3 py-4 font-semibold">{row.delivered}</td><td className="border-b border-[#f0f1f3] px-3 py-4">{formatINR(row.revenue)}</td><td className="border-b border-[#f0f1f3] px-3 py-4">{formatPct(row.conversion)}</td><td className="border-b border-[#f0f1f3] px-3 py-4"><span className={row.highIntentStale ? "font-bold text-[#b83232]" : "text-[#667085]"}>{row.stale}</span></td><td className="border-b border-[#f0f1f3] px-3 py-4 text-right"><Link href={`/reps/${row.rep.id}?from=${filters.from}&to=${filters.to}`} className="inline-flex items-center gap-1 text-xs font-bold text-[#315fce]">Coach <ArrowRight size={13}/></Link></td></tr>)}</tbody></table></div>;
}
